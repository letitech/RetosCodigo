#Área de un rectángulo dado su base y altura (a=base*altura)

base = float(input("Introduce la base: "))
altura = float(input("Introduce la altura: "))

area = base*altura

print("El área del rectángulo es", area)